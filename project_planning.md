# 旧手机家用摄像头项目规划

> 本文档已根据代码实际功能同步更新（2026-08-03）。需求冲突时以 README.md 最终版为准。

## 一、项目概述

将旧 Android 手机变为局域网监控摄像头，具备实时画面查看、运动检测报警、自动录像、后台保活等功能。  
**特点**：完全本地运行，无需第三方云服务，免费且开源。

---

## 二、已实现核心功能

| 功能            | 说明                                                                     |
| ------------- | ---------------------------------------------------------------------- |
| 实时视频流 (MJPEG) | 浏览器通过 `http://IP:8080/video` 观看，支持 HTTP 基本认证，多设备同时观看互不影响              |
| 运动检测          | Y 平面亮度帧差法，画面变化时自动触发报警和录像，灵敏度可调（0-100，数值越小越敏感）                          |
| HTTP 报警推送     | 检测到运动时向用户配置的 URL 发送 JSON 数据（POST），含静默期防报警风暴                          |
| 运动触发短视频录制     | 检测到运动时自动录制 MP4（含音频），时长可选 10 秒 / 30 秒 / 1 分钟 / 3 分钟 / 5 分钟              |
| 连续录像（按时间分段）   | 按时长自动分段轮转，分段时长可选 1 分钟 / 5 分钟 / 10 分钟 / 30 分钟 / 1 小时；修改分段时长对下一片段生效      |
| 录像模式切换        | 主界面两个互斥开关实现三档：连续录像 / 运动触发录像 / 仅预览，实时生效                                |
| 监控时间限制        | 可设定每天只在特定时段内监控（支持跨天时段），时段外丢弃帧                                          |
| 控制界面          | 主界面显示 IP、运行状态、录像存储占用、实时帧率；可开关 MJPEG、切换录像模式；查看录像列表                     |
| 录像管理          | 按日期文件夹两级浏览，视频搜索、元数据显示（时长/录制时间）、多选删除、多选导出、点击播放                          |
| 后台保活          | 前台服务、唤醒锁、引导忽略电池优化                                                      |
| 开机自启          | 代码支持，需用户授权（设置中可开关）                                                     |
| 用户可配置参数       | 分辨率、帧率、灵敏度、报警 URL/静默期、监控时段、录像模式与时长、HTTP 认证；除分辨率/帧率外均支持热重载              |

---

## 三、设置页面参数说明

| 参数            | 类型   | 默认值         | 说明                                            |
| ------------- | ---- | ----------- | --------------------------------------------- |
| 推流分辨率         | 下拉选择 | 640x480     | 动态读取摄像头硬件支持的分辨率列表（带兜底列表），**修改后需重启监控生效**       |
| 推流帧率          | 滑动条  | 16 fps      | 范围 1~30，软限制，实际帧率受设备性能影响，**修改后需重启监控生效**        |
| 运动检测灵敏度       | 滑动条  | 50          | 数值越小越敏感（0-100），实时生效                           |
| 报警接收 URL      | 文本输入 | 空           | 运动报警目标地址（POST JSON），留空则不发送，实时生效               |
| 报警静默期         | 数字输入 | 30 秒        | 同一事件在静默期内不重复报警，实时生效                           |
| 运动录像时长        | 下拉选择 | 10 秒        | 可选 10 秒 / 30 秒 / 1 分钟 / 3 分钟 / 5 分钟，实时生效      |
| 连续录像分段时长      | 下拉选择 | 10 分钟       | 可选 1 分钟 / 5 分钟 / 10 分钟 / 30 分钟 / 1 小时；修改对下一片段生效 |
| 开机自启          | 开关   | 关闭          | 控制是否在开机时自动启动服务                                |
| 监控时间限制        | 时间选择 | 00:00~23:59 | 开始/结束时间，支持跨天（如 22:00~06:00）；起止相同表示全天           |
| HTTP 认证用户名/密码 | 文本输入 | 空           | 设置后浏览器需凭据才能观看视频流，留空则不认证                       |

> 热重载机制：设置保存后通过 `RELOAD_CONFIG` 广播通知服务实时重载；**唯一例外是分辨率和帧率**，在相机启动时读取一次，修改后需重启监控。

---

## 四、待完善功能（已规划，按需实现）

| 功能          | 状态      | 说明                                |
| ----------- | ------- | --------------------------------- |
| 录像存储循环覆盖    | ❌ 未实现   | 无存储上限与自动清理逻辑，存储空间用尽前需手动清理录像       |
| 摄像头切换（前后置）  | ❌ 未实现   | 当前固定后置摄像头，可增加前置选项                 |
| 画面镜像/旋转     | ❌ 未实现   | 部分安装姿态可能需要水平翻转                    |
| 推流端口可配置     | ❌ 未实现   | 当前固定 8080，高级用户可修改                 |
| 通知栏静默模式     | ❌ 未实现   | 隐藏通知图标但保留前台服务                     |

---

## 五、界面结构

### 1. 主界面

- IP 地址显示（含 8080 端口访问地址）
- 服务状态指示（运行中绿色 / 已停止红色）
- 录像存储占用（回到主界面自动刷新）
- 实时帧率（每秒更新，停止时显示 `帧率: -- fps`）
- MJPEG 推流开关（实时生效）
- 录像模式：连续录像 / 运动检测两个互斥开关（全关 = 仅预览）
- 启动/停止监控按钮（启动前依次检查相机、录音权限与电池优化）
- 查看录像按钮
- 设置按钮（进入 SettingsActivity）

### 2. 设置界面

- 视频设置：推流分辨率（Spinner，硬件动态枚举）、推流帧率（SeekBar + 数值）
- 运动检测：灵敏度（SeekBar + 数值，附帮助说明）、报警静默期、报警 URL
- 录像设置：运动录像时长（Spinner）、连续录像分段时长（Spinner），与主界面模式同步
- 系统设置：开机自启（Switch）、监控时间段（开始/结束时间）
- 安全设置：HTTP 认证用户名/密码
- 保存按钮（保存并通知服务热重载）

### 3. 录像管理界面（两级结构）

- 文件夹列表：按日期 `yyyy-MM-dd` 组织，显示视频数量与总大小；长按进入多选，支持批量删除/导出
- 视频列表：关键词搜索、显示文件名/大小/录制时间/时长；短按播放，长按多选删除/导出
- 录制时间从文件名时间戳解析（`motion_<毫秒时间戳>.mp4` / `video_<毫秒时间戳>.mp4`），解析失败时回退为文件修改时间

---

## 六、技术栈

- 语言：Kotlin（AGP 内置 Kotlin 支持）
- 最低 Android 版本：8.0 (API 26)，targetSdk 36
- 核心依赖：CameraX 1.6.1（core/camera2/lifecycle/view/video）、lifecycle-service、NanoHTTPD、OkHttp
- 组件通信：标准广播 `RELOAD_CONFIG`（ContextCompat.registerReceiver + RECEIVER_NOT_EXPORTED，进程内热重载）
- 推流：CameraX ImageAnalysis → YUV→NV21 → 字节级旋转 → JPEG 编码 → MJPEG over HTTP；每客户端独立推送线程，慢客户端自动丢帧
- 录像：CameraX VideoCapture（运动触发 + 连续分段，MP4 含音频）
- 运动检测：直接对 Y 平面降采样做帧差比较（无编解码开销），灵敏度可映射为亮度差阈值
- 报警：HTTP POST JSON to user-defined URL（含静默期）
- 存储：应用专属目录（`Android/data/.../files/Recordings/yyyy-MM-dd/`）

---

## 七、未来可扩展方向

- 远程音频监听（WebRTC / RTSP）
- 录像循环覆盖清理（可配置存储上限）
- 云存储备份（WebDAV / FTP）
- 双向语音对讲
- 多路摄像头管理
- 智能通知（仅当有人形移动时报警）

---

此文档可作为后续迭代的参考基线，所有功能均保持免费、本地优先原则。如有新增需求，可继续在现有架构上扩展。
