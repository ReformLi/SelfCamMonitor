# 数据安全说明

本文档从多个维度分析本 App（旧手机家用摄像头）的数据安全现状、风险评估以及可采取的优化措施。

* * *

## 一、MJPEG 视频流的安全

### 现状

* 有 **HTTP Basic Auth**（用户名/密码在设置中配置）
* 但传输是**明文 HTTP**（端口 8080，不是 HTTPS）

### 风险

| 风险点 | 说明  | 严重程度 |
| --- | --- | --- |
| 密码被嗅探 | 局域网内有人抓包的话，HTTP Basic Auth 的密码是 Base64 编码（不是加密），能直接看到 | ⚠️ 中 |
| 视频流被窃看 | 明文传输，同一 WiFi 下的攻击者可以直接看到你的监控画面 | ⚠️ 中高 |
| 密码强度 | 默认用户名 `admin`，如果密码设得弱（比如 123456），容易被暴力破解 | ⚠️ 中 |

### 好消息

* 本 App **只做本地局域网用**，不暴露到公网，风险可控
* 只要信任连接到你家 WiFi 的设备，就没有大问题

* * *

## 二、录像文件的存储安全

### 现状

* 录像存在 `getExternalFilesDir(null)/Recordings/yyyy-MM-dd/`
* 这是 **App 私有目录**（Android 10+ 分区存储下，其他 App 无法直接访问）

### 风险

| 风险点 | 说明  | 严重程度 |
| --- | --- | --- |
| 手机丢失/被盗 | 如果手机没设锁屏密码，拿到手机的人可以直接拷贝录像文件 | ⚠️ 中 |
| 卸载 App | 卸载后录像文件会被一起删除（私有目录的特性） | ⚠️ 低（用户自己操作） |
| 没有加密 | 录像文件是普通 MP4，没有加密，拿到文件就能播放 | ⚠️ 中 |

### 好消息

* 分区存储下，其他 App 确实看不到你的录像，安全性比存在公共目录（比如 `DCIM/`）高很多

* * *

## 三、配置数据的安全

### 现状

* 所有配置存在 `SharedPreferences` 的 `camera_prefs.xml` 文件中
* 包括：HTTP 认证的用户名/密码、报警 URL 等
* **没有加密**，是明文 XML

### 风险

| 风险点 | 说明  | 严重程度 |
| --- | --- | --- |
| Root 后的手机 | 如果手机被 Root，攻击者可以直接读到 `camera_prefs.xml`，拿到所有密码 | ⚠️ 中 |
| 密码明文存储 | HTTP 认证密码没有加密，存在泄漏风险 | ⚠️ 中低 |

### 好消息

* 非 Root 的普通手机，其他 App 无法访问你的 SharedPreferences，因为它是 `MODE_PRIVATE`

* * *

## 四、报警 URL 的安全

### 现状

* 检测到运动时，会发送 HTTP GET 请求到你配置的报警 URL
* 也是**明文 HTTP** 请求

### 风险

* 如果报警 URL 是公网地址，请求可能被中间节点看到（携带了时间戳）
* 如果报警 URL 是局域网设备（比如家里的智能家居），风险很低

* * *

## 五、整体评估

### 如果只在局域网用（本 App 的设计目标）

**安全性是可以接受的**，因为：

1. 攻击者需要先连到你家 WiFi（只有受信任的设备才能看到画面和文件）
2. 手机设了锁屏密码，丢了也不怕
3. 不暴露到公网，外部攻击者打不到

### 你需要注意的几件事

1. **WiFi 密码要够强**（至少 8 位，含数字字母符号）
2. **手机要设锁屏密码**（PIN / 图案 / 指纹）
3. **HTTP 认证密码不要和 WiFi 密码一样**
4. 不要把 `8080` 端口映射到路由器公网（除非你知道自己在做什么）

* * *

## 六、可进一步增强的安全优化（后续可选）

| 优化项 | 效果  | 改动量 |
| --- | --- | --- |
| 视频流改用 HTTPS | 防止局域网内嗅探密码和画面 | 中（需要自签名证书） |
| HTTP 认证密码加密存储 | SharedPreferences 里的密码加密 | 小   |
| 录像文件加密存储 | 拿到文件也不能直接播放 | 大（影响性能） |
| 支持 WSS / WebRTC | 端到端加密 | 很大  |

* * *

## 七、免责说明

本 App 设计目标为**本地局域网家用监控**，未针对公网暴露场景做安全审计。请勿将本 App 的视频流端口直接暴露到公网，否则所有安全风险由使用者自行承担。