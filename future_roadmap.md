# SelfCamMonitor 未来发展方向

> 基于项目现状（2026-08-04）制定，遵循「免费、本地优先、隐私优先」原则。

---

## 一、当前能力概览

| 已实现 | 说明 |
|--------|------|
| MJPEG 实时推流 | 浏览器观看，多客户端，HTTP Basic Auth |
| Web 查看页面 | 状态指示、自动重连、旋转、缩放平移、全屏 |
| 运动检测 + 报警 | Y平面帧差法，HTTP POST 报警，静默期防风暴 |
| 自动录像 | 运动触发短视频 + 连续分段录像，含音频 |
| 录像管理 | 按日期两级浏览、搜索、播放、多选删除/导出 |
| 后台保活 | 前台服务 + WakeLock + 开机自启 |
| 热重载 | 设置保存后大部分参数实时生效 |

---

## 二、短期方向（1~3 个月，完善现有体验）

### 2.1 录像存储循环覆盖

**问题**：当前没有存储管理，空间用尽前需手动清理，否则新录像写入失败。

**方案**：
- 设置中增加「最大存储占用」滑块（如 1GB~32GB）或「保留天数」（如 3天/7天/30天）
- 每次开始新录像前检查存储，超限时自动删除最旧的录像文件
- 通知栏提示「已自动清理 N 天前的录像」

**优先级**：⭐⭐⭐ 高 — 没有这个功能长期运行会出问题

### 2.2 推流端口可配置

**问题**：固定 8080，如果被占用则服务启动失败。

**方案**：
- 设置中增加端口号输入（范围 1024~65535）
- 修改后需重启监控生效（和镜头/分辨率同一类）
- 启动时检测端口占用，冲突时提示

**优先级**：⭐⭐ 中

### 2.3 通知栏静默模式

**问题**：前台服务通知始终显示图标，部分用户觉得碍眼。

**方案**：
- `NotificationChannel` 设为 `IMPORTANCE_MIN`（静默级别）
- 或提供「最小化通知」开关，关闭后通知不显示图标但仍保留前台服务

**优先级**：⭐ 低 — 体验优化

### 2.4 画面镜像翻转（推流层）

**问题**：部分安装场景（如对着镜子、倒挂安装）需要画面翻转。

**方案**：
- 设置中增加「水平镜像」「垂直翻转」开关
- 在 MJPEGStreamer 的 NV21 旋转阶段额外处理镜像
- 前端查看页面的旋转是观看端调整，推流层镜像影响所有客户端和录像

**优先级**：⭐⭐ 中

### 2.5 清理遗留代码

**问题**：`CameraService.kt` 中有早期 MediaRecorder 方案的残留方法（`stopClipRecording()` / `releaseMediaRecorder()` / `imageToNv21Scaled()` / `bitmapToNv21()` 等），已不被调用。

**方案**：
- 删除死代码
- 提取 `CameraService` 中的推流逻辑、录像逻辑、运动检测逻辑到独立类，降低单文件复杂度

**优先级**：⭐ 低 — 代码质量，不影响功能

---

## 三、中期方向（3~6 个月，功能扩展）

### 3.1 Web 端远程控制

**思路**：新增 POST 端点，让浏览器能反向控制 App。

**可暴露的操作**（从低风险到高风险）：

| 操作 | 风险 | 说明 |
|------|------|------|
| 查看状态 | 无 | 已有 `/status` |
| 手动截图 | 低 | 触发一次 `/snapshot` 保存到录像目录 |
| 手动录像 | 低 | 手动触发一段录像 |
| 切换前后摄像头 | 中 | 需要重启相机绑定 |
| 开关推流/运动检测 | 中 | 改变运行状态 |
| 修改灵敏度/录像时长 | 中 | 修改设置参数 |
| 启动/停止监控 | 高 | 完全控制服务运行 |

**安全措施**：
- 所有 POST 端点必须过 Basic Auth
- 加 CSRF 防护（页面加载时下发一次性 token，POST 请求必须携带）
- 检查 `Origin` / `Referer` 头
- 高风险操作（启停监控）要求二次确认（页面弹窗 + 请求中带 `confirm=true`）
- 记录操作日志（IP、操作、时间）

**优先级**：⭐⭐⭐ 高 — 这是用户最可能想要的下一步

### 3.2 音频监听

**思路**：让浏览器不仅能看画面，还能听到声音。

**方案 A — MJPEG 扩展**：
- 在推流中加入音频帧（AAC 编码）
- 前端用 Web Audio API 解码播放
- 优点：不改架构；缺点：MJPEG 不是标准音频容器，兼容性差

**方案 B — 独立音频流**：
- 新增 `/audio` 端点，返回独立音频流（如 WAV/PCM 持续推送）
- 前端用 `<audio>` 或 Web Audio API 播放
- 优点：标准格式，兼容好；缺点：多一条连接

**方案 C — WebSocket 实时流**：
- 用 WebSocket 推送音频帧（Opus 编码）
- 前端解码播放
- 优点：低延迟；缺点：需要引入 WebSocket 库

**推荐**：方案 B 最简单，先实现单向音频监听（手机 → 浏览器），后续再考虑双向对讲。

**优先级**：⭐⭐ 中

### 3.3 录像云备份

**思路**：运动触发的关键录像自动备份到外部存储，防止手机丢失或损坏导致录像丢失。

**支持的协议**：
- **WebDAV**：兼容坚果云、NextCloud、群晖等
- **FTP/SFTP**：兼容传统文件服务器
- **SMB**：直接推送到局域网 NAS / Windows 共享文件夹

**方案**：
- 设置中配置备份目标（协议 + 地址 + 凭据 + 保存路径）
- 运动触发录像结束后，后台异步上传
- 上传成功后可选「本地保留」或「仅远程保留」（节省空间）
- 上传失败重试 3 次，记录失败日志
- 支持仅备份运动触发录像（不备份连续录像，避免海量数据）

**优先级**：⭐⭐ 中

### 3.4 录像时间轴浏览

**问题**：当前按文件夹 + 列表浏览录像，找特定时间段的录像不够直观。

**方案**：
- 录像列表页增加时间轴视图（24 小时横轴，有录像的时段高亮）
- 运动触发录像和连续录像用不同颜色标注
- 点击时间轴上的高亮段直接跳转到对应录像
- 可以缩放时间轴（小时 → 分钟级别）

**优先级**：⭐⭐ 中

### 3.5 多路摄像头管理

**思路**：一个 App 管理多台旧手机，统一查看界面。

**方案**：
- 在查看设备上安装「管理端」App（或做成 Web 端）
- 添加多个摄像头节点（IP + 端口 + 认证）
- 统一仪表盘：同时显示多路画面缩略图
- 点击某路进入全屏查看
- 统一管理录像（按摄像头筛选）

**注意**：这会让 App 从「单机模式」变为「管理端 + 摄像头端」双角色，架构变化较大。

**优先级**：⭐ 低 — 架构改动大，需谨慎评估

---

## 四、长期方向（6~12 个月，智能化与远程化）

### 4.1 端侧 AI 人形检测

**问题**：当前运动检测是纯帧差法，窗帘晃动、光线变化、虫子飞过都会误报。

**方案**：
- 集成 TensorFlow Lite 或 ML Kit，在端侧运行轻量人形检测模型
- 运动检测先做帧差法粗筛（快），检测到变化后再跑人形检测（准）
- 只有人形出现时才触发报警和录像
- 设置中增加「仅人形报警」开关

**模型选择**：
- Google ML Kit Pose Detection（轻量，API 简单）
- COCO SSD MobileNet V1（通用目标检测，可识别人/车/动物）
- YOLOv8n 量化版（精度更高，但模型体积更大）

**性能考量**：
- 检测频率不需要每帧跑，每 1~2 秒检测一次即可
- 在 ImageAnalysis 的低分辨率流上跑检测，不影响推流
- 老手机 NPU/GPU 性能有限，模型选择需实测

**优先级**：⭐⭐⭐ 高 — 这是体验提升最大的一步

### 4.2 P2P 远程访问

**问题**：当前仅限局域网，出门在外无法查看。

**方案 A — 内网穿透（用户自建）**：
- 文档引导用户使用 frp / Tailscale / ZeroTier 等工具
- App 端不需要改动
- 优点：零开发量；缺点：用户需有一定技术能力

**方案 B — STUN/TURN P2P 打洞**：
- App 内置 STUN 客户端，通过 NAT 打洞建立 P2P 连接
- 不经过第三方服务器转发（TURN 仅为兜底）
- 需要一个轻量信令服务器（可用免费公共服务）
- 优点：直连延迟低；缺点：打洞不总成功

**方案 C — 轻量中继服务器**：
- 自建或租用一台中继服务器，App 推流到中继，查看端从中继拉流
- 优点：稳定可靠；缺点：服务器成本，且违背「完全本地」理念

**推荐**：方案 A（文档引导）作为短期方案，方案 B（P2P 打洞）作为长期方向。坚守「不依赖云服务」的核心理念。

**优先级**：⭐⭐ 中 — 需求强烈但实现复杂

### 4.3 双向语音对讲

**思路**：浏览器端说话，手机端播放；实现远程喊话功能。

**技术挑战**：
- 浏览器 → 手机：浏览器 `getUserMedia` 采集麦克风 → 编码（Opus/AAC）→ WebSocket 上传 → 手机 AudioTrack 播放
- 回声消除：手机扬声器播放的声音会被麦克风采集形成回声，需要 AEC（Acoustic Echo Cancellation）
- 延迟控制：对讲场景要求 < 300ms 单向延迟

**实现路径**：
1. 先实现单向音频监听（3.2）
2. 再实现浏览器 → 手机的单向语音
3. 最后开启双向 + AEC

**优先级**：⭐ 低 — 复杂度高，需求看场景

### 4.4 录像智能摘要

**思路**：一天的录像太多看不过来，AI 自动生成摘要。

**方案**：
- 对运动触发录像做关键帧提取（每段取 1~3 帧）
- 关键帧做人形检测，标注「有人出现」
- 生成每日摘要：时间轴 + 缩略图 + 标签
- 用户一眼看到「今天 14:32 有人经过，18:05 有人停留 3 分钟」

**优先级**：⭐ 低 — 依赖人形检测先落地

---

## 五、安全加固方向

### 5.1 认证升级

| 现状 | 改进 |
|------|------|
| HTTP Basic Auth，明文传输密码 | 改用 Token 认证（登录后发短期 token） |
| 无 HTTPS | 自签证书 HTTPS，防止局域网抓包 |
| 无失败次数限制 | 登录失败 5 次锁定 IP 5 分钟 |
| 密码明文存 SharedPreferences | 改用 EncryptedSharedPreferences |

### 5.2 推流加密

当前 MJPEG 流也是明文传输，局域网内任何设备抓包都能看到画面。如果加了 HTTPS，`/video` 流也自动加密。

### 5.3 操作审计日志

如果实现了 Web 远程控制，需要记录所有控制操作：
- 时间、来源 IP、操作类型、操作参数、成功/失败
- 日志存到本地文件，可在设置中查看

---

## 六、架构优化方向

### 6.1 CameraService 拆分

当前 `CameraService.kt` 承载了太多职责（相机管理 + 推流 + 运动检测 + 录像 + 保活 + 广播接收），建议拆分：

```
CameraService（LifecycleService，协调者）
  ├── CameraManager        — 相机绑定、切换、分辨率管理
  ├── StreamManager        — 推流控制、帧率管理、编码线程池
  ├── RecordingManager     — 录像控制（连续/运动触发/分段轮转）
  ├── MotionManager        — 运动检测调度、报警触发
  └── KeepAliveManager     — WakeLock、前台通知、电池优化
```

### 6.2 单元测试

当前项目没有测试。建议优先覆盖：
- `MotionDetector` — 不同灵敏度下的检测行为
- `MJPEGStreamer` — 多客户端推流、丢帧逻辑、NV21 旋转
- `AlertManager` — 静默期逻辑、重试逻辑
- `StreamServer` — 各端点响应、认证逻辑

### 6.3 依赖注入

引入 Hilt 或手动 DI，减少 `CameraService` 中的手动对象创建，提高可测试性。

---

## 七、优先级总览

| 优先级 | 方向 | 预期效果 |
|--------|------|---------|
| P0 | 录像存储循环覆盖 | 解决长期运行的致命问题 |
| P0 | 端侧 AI 人形检测 | 从「能用」到「好用」的关键跨越 |
| P1 | Web 端远程控制 | 浏览器直接操控，体验大幅提升 |
| P1 | 安全加固（Token + HTTPS） | 反向控制的前提条件 |
| P1 | 录像云备份 | 关键录像不丢失 |
| P2 | 音频监听 | 补全「看+听」能力 |
| P2 | 推流端口可配置 | 消除端口冲突问题 |
| P2 | 录像时间轴浏览 | 录像查找体验提升 |
| P2 | 画面镜像翻转 | 适应更多安装场景 |
| P3 | P2P 远程访问 | 突出局域网限制 |
| P3 | CameraService 拆分 | 代码可维护性 |
| P3 | 单元测试 | 代码质量保障 |
| P3 | 双向语音对讲 | 远程喊话场景 |
| P3 | 录像智能摘要 | 依赖人形检测 |
| P3 | 多路摄像头管理 | 架构变化大 |
| P3 | 清理遗留代码 | 代码整洁度 |
| P3 | 通知栏静默模式 | 体验微调 |

---

## 八、核心理念（不变）

1. **完全本地优先** — 不依赖任何云服务，用户数据不出局域网（P2P 远程访问除外，且用户自主选择）
2. **免费开源** — 核心功能永远免费
3. **隐私至上** — 画面、声音、录像全部存本地，不上传任何服务器
4. **旧设备复用** — 最低支持 Android 8.0，让 5 年前的手机也能当摄像头
5. **低功耗运行** — 持续优化 CPU/内存/电量消耗，适合 7×24 小时运行

---

*本文档随项目迭代持续更新，作为发展路线图的参考基线。*
